# Local development (optional)
# Copy to .env.local and adjust if needed. Do not commit .env.local.

# Port for dev server (default: 3000). Use 443 for https://localhost (may require admin/sudo).
# PORT=3000

# URL prefix for local dev only (e.g. http://localhost:3000/my-app). Leave unset in staging/production.
# BASE_PATH=/my-app

# Enable MSW in production build (e.g. for Docker/e2e). Normally unset; dev uses MSW by default.
# NEXT_PUBLIC_ENABLE_MSW=1
